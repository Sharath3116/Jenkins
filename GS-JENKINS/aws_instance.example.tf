resource "aws_instance" "example" {
  # No need to define everything before import,
  # but after import, you must match config to actual state
}

resource "aws_instance" "my_instance" {
  # placeholder for import
}
